/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsListRoles } from "../../funcs/permissionsListRoles.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2PermissionsListRolesRequestBody$inboundSchema,
};

export const tool$permissionsListRoles: ToolDefinition<typeof args> = {
  name: "permissions-list-roles",
  description: `List roles

Retrieve all roles in your workspace including their assigned permissions. Results are paginated and sorted alphabetically by role name.

**Required Permissions**

Your root key must have the following permission:
- \`rbac.*.read_role\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsListRoles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
