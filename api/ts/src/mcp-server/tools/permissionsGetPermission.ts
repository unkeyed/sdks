/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsGetPermission } from "../../funcs/permissionsGetPermission.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2PermissionsGetPermissionRequestBody$inboundSchema,
};

export const tool$permissionsGetPermission: ToolDefinition<typeof args> = {
  name: "permissions-get-permission",
  description: `Get permission

Retrieve details about a specific permission including its name, description, and metadata.

**Required Permissions**

Your root key must have the following permission:
- \`rbac.*.read_permission\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsGetPermission(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
