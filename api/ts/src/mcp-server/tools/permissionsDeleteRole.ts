/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsDeleteRole } from "../../funcs/permissionsDeleteRole.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2PermissionsDeleteRoleRequestBody$inboundSchema,
};

export const tool$permissionsDeleteRole: ToolDefinition<typeof args> = {
  name: "permissions-delete-role",
  description: `Delete role

Remove a role from your workspace. This also removes the role from all assigned API keys.

**Important:** This operation cannot be undone and immediately affects all API keys that had this role assigned.

**Required Permissions**

Your root key must have the following permission:
- \`rbac.*.delete_role\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsDeleteRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
