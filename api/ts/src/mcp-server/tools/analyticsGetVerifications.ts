/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { analyticsGetVerifications } from "../../funcs/analyticsGetVerifications.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2AnalyticsGetVerificationsRequestBody$inboundSchema,
};

export const tool$analyticsGetVerifications: ToolDefinition<typeof args> = {
  name: "analytics-get-verifications",
  description: `Query key verification data

Execute custom SQL queries against your key verification analytics.
For complete documentation including available tables, columns, data types, query examples, see the schema reference in the API documentation.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await analyticsGetVerifications(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
