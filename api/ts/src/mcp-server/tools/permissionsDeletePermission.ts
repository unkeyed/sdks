/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsDeletePermission } from "../../funcs/permissionsDeletePermission.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2PermissionsDeletePermissionRequestBody$inboundSchema,
};

export const tool$permissionsDeletePermission: ToolDefinition<typeof args> = {
  name: "permissions-delete-permission",
  description: `Delete permission

Remove a permission from your workspace. This also removes the permission from all API keys and roles.

**Important:** This operation cannot be undone and immediately affects all API keys and roles that had this permission assigned.

**Required Permissions**

Your root key must have the following permission:
- \`rbac.*.delete_permission\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsDeletePermission(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
