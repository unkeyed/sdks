/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { permissionsListPermissions } from "../../funcs/permissionsListPermissions.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.V2PermissionsListPermissionsRequestBody$inboundSchema,
};

export const tool$permissionsListPermissions: ToolDefinition<typeof args> = {
  name: "permissions-list-permissions",
  description: `List permissions

Retrieve all permissions in your workspace. 
Results are paginated and sorted by their id.

**Required Permissions**

Your root key must have the following permission:
- \`rbac.*.read_permission\`
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await permissionsListPermissions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
