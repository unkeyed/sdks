"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from typing import Any, Iterable, List, Mapping, Optional, Union
from unkey.py import errors, models, utils
from unkey.py._hooks import HookContext
from unkey.py.types import OptionalNullable, UNSET
from unkey.py.utils.unmarshal_json_response import unmarshal_json_response


class Environments(BaseSDK):
    r"""Environment management operations"""

    def get_environment(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsGetEnvironmentResponseBody:
        r"""Get environment

        Retrieve a single environment by its id.

        Use this to fetch environment details after creation or to verify an environment exists before performing operations.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.read_environment` (to read any environment)
        - `environment.<environment_id>.read_environment` (to read a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsGetEnvironmentRequestBody(
            project=project,
            app=app,
            environment=environment,
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.getEnvironment",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsGetEnvironmentRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.getEnvironment",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsGetEnvironmentResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def get_environment_async(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsGetEnvironmentResponseBody:
        r"""Get environment

        Retrieve a single environment by its id.

        Use this to fetch environment details after creation or to verify an environment exists before performing operations.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.read_environment` (to read any environment)
        - `environment.<environment_id>.read_environment` (to read a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsGetEnvironmentRequestBody(
            project=project,
            app=app,
            environment=environment,
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.getEnvironment",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsGetEnvironmentRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.getEnvironment",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsGetEnvironmentResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    def list_environment_variables(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        limit: Optional[int] = 100,
        cursor: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsListEnvironmentVariablesResponseBody:
        r"""List environment variables

        Retrieve the environment variables for an environment.

        Identify the environment by its project, app, and environment identifiers.
        Results are ordered by variable id and paginated; when `hasMore` is true,
        pass the returned `cursor` to fetch the next page.

        `recoverable` variables are returned with their decrypted plaintext value.
        `writeonly` variables never expose a value: only the key, kind, and
        description are returned.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.read_environment_variables` (for any environment)
        - `environment.<environment_id>.read_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param limit: Maximum number of variables to return per request.
        :param cursor: Pagination cursor from a previous response to fetch the next page.
            Use when `hasMore: true` in the previous response.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsListEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            limit=limit,
            cursor=cursor,
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.listEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsListEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.listEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsListEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def list_environment_variables_async(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        limit: Optional[int] = 100,
        cursor: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsListEnvironmentVariablesResponseBody:
        r"""List environment variables

        Retrieve the environment variables for an environment.

        Identify the environment by its project, app, and environment identifiers.
        Results are ordered by variable id and paginated; when `hasMore` is true,
        pass the returned `cursor` to fetch the next page.

        `recoverable` variables are returned with their decrypted plaintext value.
        `writeonly` variables never expose a value: only the key, kind, and
        description are returned.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.read_environment_variables` (for any environment)
        - `environment.<environment_id>.read_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param limit: Maximum number of variables to return per request.
        :param cursor: Pagination cursor from a previous response to fetch the next page.
            Use when `hasMore: true` in the previous response.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsListEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            limit=limit,
            cursor=cursor,
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.listEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsListEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.listEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsListEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    def list_environments(
        self,
        *,
        project: str,
        app: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsListEnvironmentsResponseBody:
        r"""List environments

        Retrieve the environments within an app.

        Use this to enumerate every environment in an app. Identify the app by its project slug and app slug. Results are ordered by environment id. An app has only a handful of environments, so all of them are returned in a single response.

        **Required Permissions**

        Your root key must have the following permission:
        - `environment.*.read_environment` (to read environments in any app)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsListEnvironmentsRequestBody(
            project=project,
            app=app,
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.listEnvironments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsListEnvironmentsRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.listEnvironments",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsListEnvironmentsResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def list_environments_async(
        self,
        *,
        project: str,
        app: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsListEnvironmentsResponseBody:
        r"""List environments

        Retrieve the environments within an app.

        Use this to enumerate every environment in an app. Identify the app by its project slug and app slug. Results are ordered by environment id. An app has only a handful of environments, so all of them are returned in a single response.

        **Required Permissions**

        Your root key must have the following permission:
        - `environment.*.read_environment` (to read environments in any app)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsListEnvironmentsRequestBody(
            project=project,
            app=app,
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.listEnvironments",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsListEnvironmentsRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.listEnvironments",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsListEnvironmentsResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    def remove_environment_variables(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        variables: Iterable[str],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsRemoveEnvironmentVariablesResponseBody:
        r"""Remove environment variables

        Remove environment variables from an environment in a single atomic request.

        This operation only deletes keys by name. Keys in the payload that exist are
        removed; keys that are not present are ignored, since their absence already
        matches the requested state. To replace or update values, use
        `setEnvironmentVariables` instead. The whole operation is atomic: if any part
        fails the environment is left unchanged.

        Duplicate keys in the payload collapse to a single removal. Values are never
        read or returned.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.remove_environment_variables` (for any environment)
        - `environment.<environment_id>.remove_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param variables: The names of the variables to remove. Keys that exist are deleted; keys
            that are not present are ignored, since their absence already matches the
            requested state.

            Duplicate keys collapse to a single removal. The whole operation is atomic:
            if any part fails the environment is left unchanged. Limited to 50
            variables per request.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsRemoveEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            variables=utils.unmarshal(variables, List[str]),
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.removeEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsRemoveEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.removeEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsRemoveEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def remove_environment_variables_async(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        variables: Iterable[str],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsRemoveEnvironmentVariablesResponseBody:
        r"""Remove environment variables

        Remove environment variables from an environment in a single atomic request.

        This operation only deletes keys by name. Keys in the payload that exist are
        removed; keys that are not present are ignored, since their absence already
        matches the requested state. To replace or update values, use
        `setEnvironmentVariables` instead. The whole operation is atomic: if any part
        fails the environment is left unchanged.

        Duplicate keys in the payload collapse to a single removal. Values are never
        read or returned.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.remove_environment_variables` (for any environment)
        - `environment.<environment_id>.remove_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param variables: The names of the variables to remove. Keys that exist are deleted; keys
            that are not present are ignored, since their absence already matches the
            requested state.

            Duplicate keys collapse to a single removal. The whole operation is atomic:
            if any part fails the environment is left unchanged. Limited to 50
            variables per request.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsRemoveEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            variables=utils.unmarshal(variables, List[str]),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.removeEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsRemoveEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.removeEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsRemoveEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    def set_environment_variables(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        variables: Union[
            Iterable[models.EnvironmentVariableInput],
            Iterable[models.EnvironmentVariableInputTypedDict],
        ],
        prune: Optional[bool] = False,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsSetEnvironmentVariablesResponseBody:
        r"""Set environment variables

        Create or update environment variables for an environment in a single atomic
        request.

        By default this is an upsert: each variable in the payload is created if new
        or fully overwritten if the key already exists, and any variable not in the
        payload is left untouched. This lets you change one variable without
        re-sending the others, which matters for write-only secrets you can no longer
        read back.

        Set `prune: true` to make it a full replace instead: after upserting, every
        variable not in the payload is deleted. Sending `prune: true` with an empty
        `variables` list resets the environment by deleting every variable.

        Each variable is written exactly as sent, never merged, so omitted optional
        fields fall back to their defaults rather than the previous value. Values are
        always encrypted at rest. Set `kind: recoverable` to allow a value to be read
        back; it defaults to `writeonly`, which can never be read back through the API.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.set_environment_variables` (for any environment)
        - `environment.<environment_id>.set_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param variables: The variables to upsert. Each entry is created if its key is new or fully
            overwritten if the key already exists. Existing variables whose keys are
            not in this list are left untouched, unless `prune` is true.

            Each entry is written exactly as sent, never merged with the current
            state. Only `value` is required; omitted optional fields (`kind`,
            `description`) fall back to their defaults rather than any previous value,
            so overwriting a variable without a `description` clears it.

            Each key may appear at most once; a duplicate key is rejected with a 400.
            The whole operation is atomic: if any part fails the environment is left
            unchanged. All values are encrypted at rest. Limited to 50 variables per
            request.

        :param prune: Optional. Defaults to false. When false, the variables above are upserted
            and any existing variable not in the list is kept. When true, this becomes
            a full replace: after upserting, every variable not in the list is deleted.
            Combined with an empty `variables` list, `prune: true` resets the entire
            environment by deleting every variable.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsSetEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            variables=utils.get_pydantic_model(
                variables, List[models.EnvironmentVariableInput]
            ),
            prune=prune,
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.setEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsSetEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.setEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsSetEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def set_environment_variables_async(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        variables: Union[
            Iterable[models.EnvironmentVariableInput],
            Iterable[models.EnvironmentVariableInputTypedDict],
        ],
        prune: Optional[bool] = False,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsSetEnvironmentVariablesResponseBody:
        r"""Set environment variables

        Create or update environment variables for an environment in a single atomic
        request.

        By default this is an upsert: each variable in the payload is created if new
        or fully overwritten if the key already exists, and any variable not in the
        payload is left untouched. This lets you change one variable without
        re-sending the others, which matters for write-only secrets you can no longer
        read back.

        Set `prune: true` to make it a full replace instead: after upserting, every
        variable not in the payload is deleted. Sending `prune: true` with an empty
        `variables` list resets the environment by deleting every variable.

        Each variable is written exactly as sent, never merged, so omitted optional
        fields fall back to their defaults rather than the previous value. Values are
        always encrypted at rest. Set `kind: recoverable` to allow a value to be read
        back; it defaults to `writeonly`, which can never be read back through the API.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.set_environment_variables` (for any environment)
        - `environment.<environment_id>.set_environment_variables` (for a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param variables: The variables to upsert. Each entry is created if its key is new or fully
            overwritten if the key already exists. Existing variables whose keys are
            not in this list are left untouched, unless `prune` is true.

            Each entry is written exactly as sent, never merged with the current
            state. Only `value` is required; omitted optional fields (`kind`,
            `description`) fall back to their defaults rather than any previous value,
            so overwriting a variable without a `description` clears it.

            Each key may appear at most once; a duplicate key is rejected with a 400.
            The whole operation is atomic: if any part fails the environment is left
            unchanged. All values are encrypted at rest. Limited to 50 variables per
            request.

        :param prune: Optional. Defaults to false. When false, the variables above are upserted
            and any existing variable not in the list is kept. When true, this becomes
            a full replace: after upserting, every variable not in the list is deleted.
            Combined with an empty `variables` list, `prune: true` resets the entire
            environment by deleting every variable.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsSetEnvironmentVariablesRequestBody(
            project=project,
            app=app,
            environment=environment,
            variables=utils.get_pydantic_model(
                variables, List[models.EnvironmentVariableInput]
            ),
            prune=prune,
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.setEnvironmentVariables",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsSetEnvironmentVariablesRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.setEnvironmentVariables",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsSetEnvironmentVariablesResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    def update_settings(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        dockerfile: OptionalNullable[str] = UNSET,
        root_directory: Optional[str] = None,
        build_command: OptionalNullable[str] = UNSET,
        watch_paths: Optional[Iterable[str]] = None,
        auto_deploy: Optional[bool] = None,
        port: Optional[int] = None,
        v_cpus: Optional[float] = None,
        memory_mib: Optional[int] = None,
        storage_mib: Optional[int] = None,
        command: Optional[Iterable[str]] = None,
        healthcheck: OptionalNullable[
            Union[models.EnvironmentHealthcheck, models.EnvironmentHealthcheckTypedDict]
        ] = UNSET,
        shutdown_signal: Optional[models.EnvironmentShutdownSignal] = None,
        upstream_protocol: Optional[models.EnvironmentUpstreamProtocol] = None,
        openapi_spec_path: OptionalNullable[str] = UNSET,
        regions: Optional[
            Union[
                Iterable[models.EnvironmentRegion],
                Iterable[models.EnvironmentRegionTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsUpdateSettingsResponseBody:
        r"""Update environment settings

        Update the build, runtime, and regional settings for an environment.

        All settings fields are optional. Omit a field to leave it unchanged. For
        nullable fields (`dockerfile`, `healthcheck`, `openapiSpecPath`), send null
        to clear the value. When `regions` is present it replaces the full set of
        regions for the environment.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.update_environment` (to update any environment)
        - `environment.<environment_id>.update_environment` (to update a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param dockerfile: Path to the Dockerfile used for builds.
            Omit to leave unchanged; set null to clear and fall back to Railpack.

        :param root_directory: The directory your app lives in. Unkey builds from here.
            Use \".\" for the repository root, or set a subdirectory when your app
            is nested (e.g., services/api). Omit to leave unchanged.

        :param build_command: Overrides the build command auto-detected by Railpack.
            Omit to leave unchanged; set null to clear and fall back to auto-detection.

        :param watch_paths: Glob paths that trigger auto-deploys when changed.
            Omit to leave unchanged.

        :param auto_deploy: Whether pushes auto-deploy.
            Omit to leave unchanged.

        :param port: Container port the app listens on.
            Omit to leave unchanged.

        :param v_cpus: CPU allocation in vCPUs. Minimum 0.25 (1/4 vCPU), in steps of 0.25.
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param memory_mib: Memory allocation in MiB. Minimum 256, in steps of 256.
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param storage_mib: Ephemeral storage allocation in MiB, in steps of 512 (0 for none).
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param command: Override container entrypoint command.
            Omit to leave unchanged.

        :param healthcheck: HTTP healthcheck configuration.
            Omit to leave unchanged; set null to remove.

        :param shutdown_signal: Signal sent to the container on shutdown.

        :param upstream_protocol: Protocol used to reach the container.

        :param openapi_spec_path: Path to the OpenAPI spec file within the build. Must start with a slash.
            Omit to leave unchanged; set null to clear.

        :param regions: Desired set of regions with per-region replica bounds.
            Omit to leave regions unchanged; when present, this replaces the full set
            (regions absent from the list are removed). At least one region is required;
            an empty list is rejected because an environment cannot have zero regions.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsUpdateSettingsRequestBody(
            project=project,
            app=app,
            environment=environment,
            dockerfile=dockerfile,
            root_directory=root_directory,
            build_command=build_command,
            watch_paths=utils.unmarshal(watch_paths, Optional[List[str]]),
            auto_deploy=auto_deploy,
            port=port,
            v_cpus=v_cpus,
            memory_mib=memory_mib,
            storage_mib=storage_mib,
            command=utils.unmarshal(command, Optional[List[str]]),
            healthcheck=utils.get_pydantic_model(
                healthcheck, OptionalNullable[models.EnvironmentHealthcheck]
            ),
            shutdown_signal=shutdown_signal,
            upstream_protocol=upstream_protocol,
            openapi_spec_path=openapi_spec_path,
            regions=utils.get_pydantic_model(
                regions, Optional[List[models.EnvironmentRegion]]
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/environments.updateSettings",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsUpdateSettingsRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.updateSettings",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsUpdateSettingsResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)

    async def update_settings_async(
        self,
        *,
        project: str,
        app: str,
        environment: str,
        dockerfile: OptionalNullable[str] = UNSET,
        root_directory: Optional[str] = None,
        build_command: OptionalNullable[str] = UNSET,
        watch_paths: Optional[Iterable[str]] = None,
        auto_deploy: Optional[bool] = None,
        port: Optional[int] = None,
        v_cpus: Optional[float] = None,
        memory_mib: Optional[int] = None,
        storage_mib: Optional[int] = None,
        command: Optional[Iterable[str]] = None,
        healthcheck: OptionalNullable[
            Union[models.EnvironmentHealthcheck, models.EnvironmentHealthcheckTypedDict]
        ] = UNSET,
        shutdown_signal: Optional[models.EnvironmentShutdownSignal] = None,
        upstream_protocol: Optional[models.EnvironmentUpstreamProtocol] = None,
        openapi_spec_path: OptionalNullable[str] = UNSET,
        regions: Optional[
            Union[
                Iterable[models.EnvironmentRegion],
                Iterable[models.EnvironmentRegionTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.V2EnvironmentsUpdateSettingsResponseBody:
        r"""Update environment settings

        Update the build, runtime, and regional settings for an environment.

        All settings fields are optional. Omit a field to leave it unchanged. For
        nullable fields (`dockerfile`, `healthcheck`, `openapiSpecPath`), send null
        to clear the value. When `regions` is present it replaces the full set of
        regions for the environment.

        **Required Permissions**

        Your root key must have one of the following permissions:
        - `environment.*.update_environment` (to update any environment)
        - `environment.<environment_id>.update_environment` (to update a specific environment)


        If set, this operation will use `root_key` from the global security.

        :param project: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param app: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param environment: Identifies a resource by either its unique ID or its slug.
            Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

        :param dockerfile: Path to the Dockerfile used for builds.
            Omit to leave unchanged; set null to clear and fall back to Railpack.

        :param root_directory: The directory your app lives in. Unkey builds from here.
            Use \".\" for the repository root, or set a subdirectory when your app
            is nested (e.g., services/api). Omit to leave unchanged.

        :param build_command: Overrides the build command auto-detected by Railpack.
            Omit to leave unchanged; set null to clear and fall back to auto-detection.

        :param watch_paths: Glob paths that trigger auto-deploys when changed.
            Omit to leave unchanged.

        :param auto_deploy: Whether pushes auto-deploy.
            Omit to leave unchanged.

        :param port: Container port the app listens on.
            Omit to leave unchanged.

        :param v_cpus: CPU allocation in vCPUs. Minimum 0.25 (1/4 vCPU), in steps of 0.25.
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param memory_mib: Memory allocation in MiB. Minimum 256, in steps of 256.
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param storage_mib: Ephemeral storage allocation in MiB, in steps of 512 (0 for none).
            The upper bound is your workspace's per-instance quota; exceeding it returns 400.
            Omit to leave unchanged.

        :param command: Override container entrypoint command.
            Omit to leave unchanged.

        :param healthcheck: HTTP healthcheck configuration.
            Omit to leave unchanged; set null to remove.

        :param shutdown_signal: Signal sent to the container on shutdown.

        :param upstream_protocol: Protocol used to reach the container.

        :param openapi_spec_path: Path to the OpenAPI spec file within the build. Must start with a slash.
            Omit to leave unchanged; set null to clear.

        :param regions: Desired set of regions with per-region replica bounds.
            Omit to leave regions unchanged; when present, this replaces the full set
            (regions absent from the list are removed). At least one region is required;
            an empty list is rejected because an environment cannot have zero regions.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.V2EnvironmentsUpdateSettingsRequestBody(
            project=project,
            app=app,
            environment=environment,
            dockerfile=dockerfile,
            root_directory=root_directory,
            build_command=build_command,
            watch_paths=utils.unmarshal(watch_paths, Optional[List[str]]),
            auto_deploy=auto_deploy,
            port=port,
            v_cpus=v_cpus,
            memory_mib=memory_mib,
            storage_mib=storage_mib,
            command=utils.unmarshal(command, Optional[List[str]]),
            healthcheck=utils.get_pydantic_model(
                healthcheck, OptionalNullable[models.EnvironmentHealthcheck]
            ),
            shutdown_signal=shutdown_signal,
            upstream_protocol=upstream_protocol,
            openapi_spec_path=openapi_spec_path,
            regions=utils.get_pydantic_model(
                regions, Optional[List[models.EnvironmentRegion]]
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/environments.updateSettings",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request,
                False,
                False,
                "json",
                models.V2EnvironmentsUpdateSettingsRequestBody,
            ),
            allow_empty_value=None,
            allowed_fields=["root_key"],
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config
            else:
                retries = utils.RetryConfig(
                    "backoff", utils.BackoffStrategy(50, 1000, 1.5, 10000), True
                )

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["5XX"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="environments.updateSettings",
                oauth2_scopes=None,
                security_source=self.sdk_configuration.security,
                tags=["environments"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.V2EnvironmentsUpdateSettingsResponseBody, http_res
            )
        if utils.match_response(http_res, "400", "application/json"):
            response_data = unmarshal_json_response(
                errors.BadRequestErrorResponseData, http_res
            )
            raise errors.BadRequestErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "401", "application/json"):
            response_data = unmarshal_json_response(
                errors.UnauthorizedErrorResponseData, http_res
            )
            raise errors.UnauthorizedErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "403", "application/json"):
            response_data = unmarshal_json_response(
                errors.ForbiddenErrorResponseData, http_res
            )
            raise errors.ForbiddenErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "404", "application/json"):
            response_data = unmarshal_json_response(
                errors.NotFoundErrorResponseData, http_res
            )
            raise errors.NotFoundErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "429", "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.TooManyRequestsErrorResponseData, http_res
            )
            raise errors.TooManyRequestsErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "500", "application/json"):
            response_data = unmarshal_json_response(
                errors.InternalServerErrorResponseData, http_res
            )
            raise errors.InternalServerErrorResponse(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.APIError("API error occurred", http_res, http_res_text)

        raise errors.APIError("Unexpected response received", http_res)
