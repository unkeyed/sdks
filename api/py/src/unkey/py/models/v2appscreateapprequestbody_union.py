"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .appgitcreateinput import AppGitCreateInput, AppGitCreateInputTypedDict
from .appoci import AppOCI, AppOCITypedDict
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2AppsCreateAppRequestBody2TypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    name: str
    r"""Human-readable name for this app."""
    slug: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    oci: AppOCITypedDict
    r"""An app's OCI image source."""
    git: NotRequired[AppGitCreateInputTypedDict]
    r"""Configure Git as the app source. Provide `repository` to connect it during
    creation, or use an empty object to connect a repository later.

    """


class V2AppsCreateAppRequestBody2(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    name: str
    r"""Human-readable name for this app."""

    slug: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    oci: AppOCI
    r"""An app's OCI image source."""

    git: Optional[AppGitCreateInput] = None
    r"""Configure Git as the app source. Provide `repository` to connect it during
    creation, or use an empty object to connect a repository later.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["git"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2AppsCreateAppRequestBody1TypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    name: str
    r"""Human-readable name for this app."""
    slug: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    git: AppGitCreateInputTypedDict
    r"""Configure Git as the app source. Provide `repository` to connect it during
    creation, or use an empty object to connect a repository later.

    """
    oci: NotRequired[AppOCITypedDict]
    r"""An app's OCI image source."""


class V2AppsCreateAppRequestBody1(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    name: str
    r"""Human-readable name for this app."""

    slug: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    git: AppGitCreateInput
    r"""Configure Git as the app source. Provide `repository` to connect it during
    creation, or use an empty object to connect a repository later.

    """

    oci: Optional[AppOCI] = None
    r"""An app's OCI image source."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["oci"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


V2AppsCreateAppRequestBodyUnionTypedDict = TypeAliasType(
    "V2AppsCreateAppRequestBodyUnionTypedDict",
    Union[V2AppsCreateAppRequestBody1TypedDict, V2AppsCreateAppRequestBody2TypedDict],
)
r"""Create an app with exactly one source: `git` or `oci`. Requests that omit a
source or provide both sources are rejected.

"""


V2AppsCreateAppRequestBodyUnion = TypeAliasType(
    "V2AppsCreateAppRequestBodyUnion",
    Union[V2AppsCreateAppRequestBody1, V2AppsCreateAppRequestBody2],
)
r"""Create an app with exactly one source: `git` or `oci`. Requests that omit a
source or provide both sources are rejected.

"""
