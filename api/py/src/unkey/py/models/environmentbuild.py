"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class EnvironmentBuildTypedDict(TypedDict):
    r"""Build settings that control how the app is built.
    Omitted until the environment has build settings.

    """

    root_directory: str
    r"""The directory the app is built from. \".\" for the repository root.

    """
    watch_paths: List[str]
    r"""Paths that trigger a rebuild when changed.

    """
    auto_deploy: bool
    r"""Whether pushes automatically trigger a deployment.

    """
    dockerfile: NotRequired[str]
    r"""Path to the Dockerfile used to build the app, if any.

    """
    build_command: NotRequired[str]
    r"""Overrides the build command auto-detected by Railpack, so monorepos can
    scope the build to a single app. Omitted when left to auto-detection or
    for Dockerfile builds.

    """


class EnvironmentBuild(BaseModel):
    r"""Build settings that control how the app is built.
    Omitted until the environment has build settings.

    """

    root_directory: Annotated[str, pydantic.Field(alias="rootDirectory")]
    r"""The directory the app is built from. \".\" for the repository root.

    """

    watch_paths: Annotated[List[str], pydantic.Field(alias="watchPaths")]
    r"""Paths that trigger a rebuild when changed.

    """

    auto_deploy: Annotated[bool, pydantic.Field(alias="autoDeploy")]
    r"""Whether pushes automatically trigger a deployment.

    """

    dockerfile: Optional[str] = None
    r"""Path to the Dockerfile used to build the app, if any.

    """

    build_command: Annotated[Optional[str], pydantic.Field(alias="buildCommand")] = None
    r"""Overrides the build command auto-detected by Railpack, so monorepos can
    scope the build to a single app. Omitted when left to auto-detection or
    for Dockerfile builds.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["dockerfile", "buildCommand"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    EnvironmentBuild.model_rebuild()
except NameError:
    pass
