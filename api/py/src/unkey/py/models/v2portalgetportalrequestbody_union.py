"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2PortalGetPortalRequestBody3TypedDict(TypedDict):
    app_id: str
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """
    portal: NotRequired[str]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    keyspace_id: NotRequired[str]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """


class V2PortalGetPortalRequestBody3(BaseModel):
    app_id: Annotated[str, pydantic.Field(alias="appId")]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    portal: Optional[str] = None
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    keyspace_id: Annotated[Optional[str], pydantic.Field(alias="keyspaceId")] = None
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["portal", "keyspaceId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2PortalGetPortalRequestBody2TypedDict(TypedDict):
    keyspace_id: str
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """
    portal: NotRequired[str]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app_id: NotRequired[str]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """


class V2PortalGetPortalRequestBody2(BaseModel):
    keyspace_id: Annotated[str, pydantic.Field(alias="keyspaceId")]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    portal: Optional[str] = None
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app_id: Annotated[Optional[str], pydantic.Field(alias="appId")] = None
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["portal", "appId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2PortalGetPortalRequestBody1TypedDict(TypedDict):
    portal: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    keyspace_id: NotRequired[str]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """
    app_id: NotRequired[str]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """


class V2PortalGetPortalRequestBody1(BaseModel):
    portal: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    keyspace_id: Annotated[Optional[str], pydantic.Field(alias="keyspaceId")] = None
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    app_id: Annotated[Optional[str], pydantic.Field(alias="appId")] = None
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["keyspaceId", "appId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


V2PortalGetPortalRequestBodyUnionTypedDict = TypeAliasType(
    "V2PortalGetPortalRequestBodyUnionTypedDict",
    Union[
        V2PortalGetPortalRequestBody1TypedDict,
        V2PortalGetPortalRequestBody2TypedDict,
        V2PortalGetPortalRequestBody3TypedDict,
    ],
)
r"""Name the portal directly with `portal`, or name the resource it serves with
`keyspaceId` or `appId`. Exactly one of the three is required; sending more
than one is a bad request.

"""


V2PortalGetPortalRequestBodyUnion = TypeAliasType(
    "V2PortalGetPortalRequestBodyUnion",
    Union[
        V2PortalGetPortalRequestBody1,
        V2PortalGetPortalRequestBody2,
        V2PortalGetPortalRequestBody3,
    ],
)
r"""Name the portal directly with `portal`, or name the resource it serves with
`keyspaceId` or `appId`. Exactly one of the three is required; sending more
than one is a bad request.

"""


try:
    V2PortalGetPortalRequestBody3.model_rebuild()
except NameError:
    pass
try:
    V2PortalGetPortalRequestBody2.model_rebuild()
except NameError:
    pass
try:
    V2PortalGetPortalRequestBody1.model_rebuild()
except NameError:
    pass
