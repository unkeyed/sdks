"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from typing_extensions import Annotated, TypedDict
from unkey.py.types import BaseModel


class V2GithubInstallAppResponseDataTypedDict(TypedDict):
    url: str
    r"""The GitHub App install URL. Open it in a browser to install the app and
    grant repository access. After installation GitHub returns to Unkey, which
    binds the installation and lands you in the workspace settings.

    """
    expires_at: int
    r"""Unix timestamp in milliseconds after which the URL's signed state expires
    and the link must be requested again.

    """


class V2GithubInstallAppResponseData(BaseModel):
    url: str
    r"""The GitHub App install URL. Open it in a browser to install the app and
    grant repository access. After installation GitHub returns to Unkey, which
    binds the installation and lands you in the workspace settings.

    """

    expires_at: Annotated[int, pydantic.Field(alias="expiresAt")]
    r"""Unix timestamp in milliseconds after which the URL's signed state expires
    and the link must be requested again.

    """


try:
    V2GithubInstallAppResponseData.model_rebuild()
except NameError:
    pass
