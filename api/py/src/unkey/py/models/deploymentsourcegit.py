"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class DeploymentSourceGitTypedDict(TypedDict):
    r"""Build from the app's connected GitHub repository."""

    branch: NotRequired[str]
    r"""Branch to build (its HEAD). Omit branch and commitSha to use the app's default branch."""
    commit_sha: NotRequired[str]
    r"""Commit to build (full or abbreviated SHA). Takes precedence over branch."""
    repository: NotRequired[str]
    r"""Build from a fork instead of the app's connected repository, as \"owner/repo\". Requires commitSha."""


class DeploymentSourceGit(BaseModel):
    r"""Build from the app's connected GitHub repository."""

    branch: Optional[str] = None
    r"""Branch to build (its HEAD). Omit branch and commitSha to use the app's default branch."""

    commit_sha: Annotated[Optional[str], pydantic.Field(alias="commitSha")] = None
    r"""Commit to build (full or abbreviated SHA). Takes precedence over branch."""

    repository: Optional[str] = None
    r"""Build from a fork instead of the app's connected repository, as \"owner/repo\". Requires commitSha."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["branch", "commitSha", "repository"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    DeploymentSourceGit.model_rebuild()
except NameError:
    pass
