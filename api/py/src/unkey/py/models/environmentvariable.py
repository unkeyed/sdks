"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .environmentvariablekind import EnvironmentVariableKind
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class EnvironmentVariableTypedDict(TypedDict):
    key: str
    r"""The variable name."""
    kind: EnvironmentVariableKind
    r"""How the value may be read back. `writeonly` values can never be read back
    through the API; `recoverable` values can be decrypted. Values are encrypted
    at rest either way.

    """
    created_at: int
    r"""Unix timestamp in milliseconds when the variable was created."""
    value: NotRequired[str]
    r"""The decrypted plaintext value. Present only for `recoverable` variables.
    Omitted for `writeonly` variables, which can never be read back.

    """
    description: NotRequired[str]
    r"""Human-readable description. Omitted if none was provided."""


class EnvironmentVariable(BaseModel):
    key: str
    r"""The variable name."""

    kind: EnvironmentVariableKind
    r"""How the value may be read back. `writeonly` values can never be read back
    through the API; `recoverable` values can be decrypted. Values are encrypted
    at rest either way.

    """

    created_at: Annotated[int, pydantic.Field(alias="createdAt")]
    r"""Unix timestamp in milliseconds when the variable was created."""

    value: Optional[str] = None
    r"""The decrypted plaintext value. Present only for `recoverable` variables.
    Omitted for `writeonly` variables, which can never be read back.

    """

    description: Optional[str] = None
    r"""Human-readable description. Omitted if none was provided."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["value", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    EnvironmentVariable.model_rebuild()
except NameError:
    pass
