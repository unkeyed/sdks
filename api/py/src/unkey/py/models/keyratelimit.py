"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class KeyRatelimitTypedDict(TypedDict):
    r"""A rate limit applied during key verification. `limit` and `duration`
    must be set together or both omitted; a partial pair is rejected.
    """

    name: str
    r"""Name of a rate limit configured on the key or its identity, or the name
    of the inline override defined by `limit` and `duration`.
    """
    limit: NotRequired[int]
    r"""Inline override: maximum number of operations per window. Must be set
    together with `duration`.
    """
    duration: NotRequired[int]
    r"""Inline override: window duration in milliseconds. Must be set together
    with `limit`.
    """
    cost: NotRequired[int]
    r"""Cost charged against the limit per request. Defaults to 1."""


class KeyRatelimit(BaseModel):
    r"""A rate limit applied during key verification. `limit` and `duration`
    must be set together or both omitted; a partial pair is rejected.
    """

    name: str
    r"""Name of a rate limit configured on the key or its identity, or the name
    of the inline override defined by `limit` and `duration`.
    """

    limit: Optional[int] = None
    r"""Inline override: maximum number of operations per window. Must be set
    together with `duration`.
    """

    duration: Optional[int] = None
    r"""Inline override: window duration in milliseconds. Must be set together
    with `limit`.
    """

    cost: Optional[int] = None
    r"""Cost charged against the limit per request. Defaults to 1."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["limit", "duration", "cost"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
