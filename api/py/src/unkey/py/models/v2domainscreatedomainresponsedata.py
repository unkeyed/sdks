"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .dnsrecord import DNSRecord, DNSRecordTypedDict
from .domainconnect import DomainConnect, DomainConnectTypedDict
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2DomainsCreateDomainResponseDataTypedDict(TypedDict):
    domain_id: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    dns_records: List[DNSRecordTypedDict]
    r"""Every DNS record needed to finish setting up this domain, ready to create at your provider.
    The list already accounts for whether the domain is an apex or a subdomain, so no further
    branching is needed: create each entry as given.

    One record establishes routing and one proves ownership. Create all of them: whether ownership
    can be inferred from the routing record depends on how your provider publishes it, and a name
    another workspace has already verified can only be claimed through the ownership record.
    Neither is knowable before the records exist.

    """
    domain_connect: NotRequired[DomainConnectTypedDict]
    r"""One-click setup at the domain's DNS provider. Omitted entirely when the provider does not support
    Domain Connect or discovery failed, so the object's presence is the signal that the shortcut is
    available and both of its fields are filled.

    """


class V2DomainsCreateDomainResponseData(BaseModel):
    domain_id: Annotated[str, pydantic.Field(alias="domainId")]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    dns_records: Annotated[List[DNSRecord], pydantic.Field(alias="dnsRecords")]
    r"""Every DNS record needed to finish setting up this domain, ready to create at your provider.
    The list already accounts for whether the domain is an apex or a subdomain, so no further
    branching is needed: create each entry as given.

    One record establishes routing and one proves ownership. Create all of them: whether ownership
    can be inferred from the routing record depends on how your provider publishes it, and a name
    another workspace has already verified can only be claimed through the ownership record.
    Neither is knowable before the records exist.

    """

    domain_connect: Annotated[
        Optional[DomainConnect], pydantic.Field(alias="domainConnect")
    ] = None
    r"""One-click setup at the domain's DNS provider. Omitted entirely when the provider does not support
    Domain Connect or discovery failed, so the object's presence is the signal that the shortcut is
    available and both of its fields are filled.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["domainConnect"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    V2DomainsCreateDomainResponseData.model_rebuild()
except NameError:
    pass
