"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .keylocation import KeyLocation, KeyLocationTypedDict
from .keyratelimit import KeyRatelimit, KeyRatelimitTypedDict
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class KeyauthPolicyTypedDict(TypedDict):
    r"""Verifies Unkey API keys on matching requests."""

    keyspaces: List[str]
    r"""Keyspaces to verify keys against, referenced by id. All keyspaces must
    belong to your workspace.
    """
    locations: NotRequired[List[KeyLocationTypedDict]]
    r"""Where to look for the key on incoming requests, tried in order. Defaults
    to the `Authorization Bearer` header when omitted.
    """
    permission_query: NotRequired[str]
    r"""Optional permission query the verified key must satisfy, e.g.
    `documents.read AND documents.write`.
    """
    ratelimits: NotRequired[List[KeyRatelimitTypedDict]]
    r"""Rate limits applied during key verification."""


class KeyauthPolicy(BaseModel):
    r"""Verifies Unkey API keys on matching requests."""

    keyspaces: List[str]
    r"""Keyspaces to verify keys against, referenced by id. All keyspaces must
    belong to your workspace.
    """

    locations: Optional[List[KeyLocation]] = None
    r"""Where to look for the key on incoming requests, tried in order. Defaults
    to the `Authorization Bearer` header when omitted.
    """

    permission_query: Annotated[
        Optional[str], pydantic.Field(alias="permissionQuery")
    ] = None
    r"""Optional permission query the verified key must satisfy, e.g.
    `documents.read AND documents.write`.
    """

    ratelimits: Optional[List[KeyRatelimit]] = None
    r"""Rate limits applied during key verification."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["locations", "permissionQuery", "ratelimits"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    KeyauthPolicy.model_rebuild()
except NameError:
    pass
