"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class LoggingPolicyTypedDict(TypedDict):
    r"""Adds request data to the log entries of matching requests. The gateway
    always records a basic log entry for every request: method, host, path,
    status, and latency. Each capture setting is a separate opt-in: request
    headers, response headers, request body, response body, and query data.
    The policy's `match` expressions select the requests. A policy without
    `match` expressions matches every request. If more than one enabled
    logging policy matches a request, the gateway combines their settings.
    The gateway always redacts the `Authorization` header and configured key
    locations before it stores headers or query data.
    """

    request_headers: NotRequired[bool]
    r"""Capture request headers, the user agent, and the client IP. The user
    agent and client IP are included because they identify the client.
    """
    response_headers: NotRequired[bool]
    r"""Capture response headers."""
    request_body: NotRequired[bool]
    r"""Capture the request body, up to the capture limit."""
    response_body: NotRequired[bool]
    r"""Capture the response body, up to the capture limit."""
    query: NotRequired[bool]
    r"""Capture the query string and query parameters. Query data is a
    separate opt-in because URLs can contain secrets, for example
    `?api_key=...`.
    """


class LoggingPolicy(BaseModel):
    r"""Adds request data to the log entries of matching requests. The gateway
    always records a basic log entry for every request: method, host, path,
    status, and latency. Each capture setting is a separate opt-in: request
    headers, response headers, request body, response body, and query data.
    The policy's `match` expressions select the requests. A policy without
    `match` expressions matches every request. If more than one enabled
    logging policy matches a request, the gateway combines their settings.
    The gateway always redacts the `Authorization` header and configured key
    locations before it stores headers or query data.
    """

    request_headers: Annotated[
        Optional[bool], pydantic.Field(alias="requestHeaders")
    ] = False
    r"""Capture request headers, the user agent, and the client IP. The user
    agent and client IP are included because they identify the client.
    """

    response_headers: Annotated[
        Optional[bool], pydantic.Field(alias="responseHeaders")
    ] = False
    r"""Capture response headers."""

    request_body: Annotated[Optional[bool], pydantic.Field(alias="requestBody")] = False
    r"""Capture the request body, up to the capture limit."""

    response_body: Annotated[Optional[bool], pydantic.Field(alias="responseBody")] = (
        False
    )
    r"""Capture the response body, up to the capture limit."""

    query: Optional[bool] = False
    r"""Capture the query string and query parameters. Query data is a
    separate opt-in because URLs can contain secrets, for example
    `?api_key=...`.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "requestHeaders",
                "responseHeaders",
                "requestBody",
                "responseBody",
                "query",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    LoggingPolicy.model_rebuild()
except NameError:
    pass
