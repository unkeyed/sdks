"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .deploymentstatus import DeploymentStatus
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2DeploymentsListDeploymentsRequestBodyTypedDict(TypedDict):
    r"""Filter deployments within a workspace. All filters are optional; with none
    set, every deployment in the workspace is returned, newest first.

    """

    project: NotRequired[str]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: NotRequired[str]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: NotRequired[str]
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    status: NotRequired[List[DeploymentStatus]]
    r"""Restrict results to deployments in any of the given lifecycle statuses.
    Omit to return deployments in every status.

    """
    limit: NotRequired[int]
    r"""Maximum number of deployments to return per request.
    Balance between response size and number of pagination calls needed.

    """
    cursor: NotRequired[str]
    r"""Pagination cursor from a previous response to fetch the next page.
    Use when `hasMore: true` in the previous response.

    """


class V2DeploymentsListDeploymentsRequestBody(BaseModel):
    r"""Filter deployments within a workspace. All filters are optional; with none
    set, every deployment in the workspace is returned, newest first.

    """

    project: Optional[str] = None
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: Optional[str] = None
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: Optional[str] = None
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    status: Optional[List[DeploymentStatus]] = None
    r"""Restrict results to deployments in any of the given lifecycle statuses.
    Omit to return deployments in every status.

    """

    limit: Optional[int] = 100
    r"""Maximum number of deployments to return per request.
    Balance between response size and number of pagination calls needed.

    """

    cursor: Optional[str] = None
    r"""Pagination cursor from a previous response to fetch the next page.
    Use when `hasMore: true` in the previous response.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["project", "app", "environment", "status", "limit", "cursor"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
