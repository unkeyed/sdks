"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class EnvironmentHealthcheckMethod(str, Enum):
    r"""HTTP method used to probe the container."""

    GET = "GET"
    POST = "POST"


class EnvironmentHealthcheckTypedDict(TypedDict):
    method: EnvironmentHealthcheckMethod
    r"""HTTP method used to probe the container.

    """
    path: str
    r"""HTTP path probed on the container. Must start with a slash.

    """
    interval_seconds: NotRequired[int]
    r"""How often the probe runs, in seconds. Defaults to 10 when omitted.

    """
    timeout_seconds: NotRequired[int]
    r"""Per-probe timeout, in seconds. Defaults to 5 when omitted.

    """
    failure_threshold: NotRequired[int]
    r"""Consecutive failures before the container is restarted. Defaults to 3 when omitted.

    """
    initial_delay_seconds: NotRequired[int]
    r"""Delay before the first probe runs, in seconds. Defaults to 0 when omitted.

    """


class EnvironmentHealthcheck(BaseModel):
    method: EnvironmentHealthcheckMethod
    r"""HTTP method used to probe the container.

    """

    path: str
    r"""HTTP path probed on the container. Must start with a slash.

    """

    interval_seconds: Annotated[
        Optional[int], pydantic.Field(alias="intervalSeconds")
    ] = None
    r"""How often the probe runs, in seconds. Defaults to 10 when omitted.

    """

    timeout_seconds: Annotated[
        Optional[int], pydantic.Field(alias="timeoutSeconds")
    ] = None
    r"""Per-probe timeout, in seconds. Defaults to 5 when omitted.

    """

    failure_threshold: Annotated[
        Optional[int], pydantic.Field(alias="failureThreshold")
    ] = None
    r"""Consecutive failures before the container is restarted. Defaults to 3 when omitted.

    """

    initial_delay_seconds: Annotated[
        Optional[int], pydantic.Field(alias="initialDelaySeconds")
    ] = None
    r"""Delay before the first probe runs, in seconds. Defaults to 0 when omitted.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "intervalSeconds",
                "timeoutSeconds",
                "failureThreshold",
                "initialDelaySeconds",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    EnvironmentHealthcheck.model_rebuild()
except NameError:
    pass
