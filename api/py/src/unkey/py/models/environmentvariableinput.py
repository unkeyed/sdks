"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class Kind(str, Enum):
    r"""How the value may be read back. Defaults to `writeonly`."""

    RECOVERABLE = "recoverable"
    WRITEONLY = "writeonly"


class EnvironmentVariableInputTypedDict(TypedDict):
    r"""A single environment variable to set."""

    key: str
    r"""The variable name. Must be a POSIX shell name: letters, digits, and
    underscores only, and must not start with a digit. Other names are
    unreachable from shells and most runtimes.

    """
    value: str
    r"""The variable value. Always encrypted at rest. The limit is enforced
    server-side in UTF-8 bytes, so a multibyte value may be rejected before
    it reaches this code-point maximum.

    """
    kind: NotRequired[Kind]
    r"""How the value may be read back. Defaults to `writeonly`.

    """
    description: NotRequired[str]
    r"""Human-readable description of the variable.

    """


class EnvironmentVariableInput(BaseModel):
    r"""A single environment variable to set."""

    key: str
    r"""The variable name. Must be a POSIX shell name: letters, digits, and
    underscores only, and must not start with a digit. Other names are
    unreachable from shells and most runtimes.

    """

    value: str
    r"""The variable value. Always encrypted at rest. The limit is enforced
    server-side in UTF-8 bytes, so a multibyte value may be rejected before
    it reaches this code-point maximum.

    """

    kind: Optional[Kind] = Kind.WRITEONLY
    r"""How the value may be read back. Defaults to `writeonly`.

    """

    description: Optional[str] = None
    r"""Human-readable description of the variable.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["kind", "description"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
