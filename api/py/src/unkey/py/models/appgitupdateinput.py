"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class AppGitUpdateInputTypedDict(TypedDict):
    repository: NotRequired[str]
    r"""The GitHub repository to connect, as \"owner/repo\". Omit to change only the
    tracked branch of the already-connected repository. The workspace must have
    the Unkey GitHub App installed with access to it.

    """
    default_branch: NotRequired[str]
    r"""The branch this app's deployments track. Omit to adopt the repository's
    GitHub default branch on a new connection, keep the current branch when
    replacing an already-connected repository, or set it alone to retarget the
    branch of the connected repository.

    """


class AppGitUpdateInput(BaseModel):
    repository: Optional[str] = None
    r"""The GitHub repository to connect, as \"owner/repo\". Omit to change only the
    tracked branch of the already-connected repository. The workspace must have
    the Unkey GitHub App installed with access to it.

    """

    default_branch: Annotated[Optional[str], pydantic.Field(alias="defaultBranch")] = (
        None
    )
    r"""The branch this app's deployments track. Omit to adopt the repository's
    GitHub default branch on a new connection, keep the current branch when
    replacing an already-connected repository, or set it alone to retarget the
    branch of the connected repository.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["repository", "defaultBranch"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    AppGitUpdateInput.model_rebuild()
except NameError:
    pass
