"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .environmenthealthcheck import (
    EnvironmentHealthcheck,
    EnvironmentHealthcheckTypedDict,
)
from .environmentregion import EnvironmentRegion, EnvironmentRegionTypedDict
from .environmentshutdownsignal import EnvironmentShutdownSignal
from .environmentupstreamprotocol import EnvironmentUpstreamProtocol
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class V2EnvironmentsUpdateSettingsRequestBodyTypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    dockerfile: NotRequired[Nullable[str]]
    r"""Path to the Dockerfile used for builds.
    Omit to leave unchanged; set null to clear and fall back to Railpack.

    """
    root_directory: NotRequired[str]
    r"""The directory your app lives in. Unkey builds from here.
    Use \".\" for the repository root, or set a subdirectory when your app
    is nested (e.g., services/api). Omit to leave unchanged.

    """
    build_command: NotRequired[Nullable[str]]
    r"""Overrides the build command auto-detected by Railpack.
    Omit to leave unchanged; set null to clear and fall back to auto-detection.

    """
    watch_paths: NotRequired[List[str]]
    r"""Glob paths that trigger auto-deploys when changed.
    Omit to leave unchanged.

    """
    auto_deploy: NotRequired[bool]
    r"""Whether pushes auto-deploy.
    Omit to leave unchanged.

    """
    port: NotRequired[int]
    r"""Container port the app listens on.
    Omit to leave unchanged.

    """
    v_cpus: NotRequired[float]
    r"""CPU allocation in vCPUs. Minimum 0.25 (1/4 vCPU), in steps of 0.25.
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """
    memory_mib: NotRequired[int]
    r"""Memory allocation in MiB. Minimum 256, in steps of 256.
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """
    storage_mib: NotRequired[int]
    r"""Ephemeral storage allocation in MiB, in steps of 512 (0 for none).
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """
    command: NotRequired[List[str]]
    r"""Override container entrypoint command.
    Omit to leave unchanged.

    """
    healthcheck: NotRequired[Nullable[EnvironmentHealthcheckTypedDict]]
    r"""HTTP healthcheck configuration.
    Omit to leave unchanged; set null to remove.

    """
    shutdown_signal: NotRequired[EnvironmentShutdownSignal]
    r"""Signal sent to the container on shutdown.

    """
    upstream_protocol: NotRequired[EnvironmentUpstreamProtocol]
    r"""Protocol used to reach the container.

    """
    openapi_spec_path: NotRequired[Nullable[str]]
    r"""Path to the OpenAPI spec file within the build. Must start with a slash.
    Omit to leave unchanged; set null to clear.

    """
    regions: NotRequired[List[EnvironmentRegionTypedDict]]
    r"""Desired set of regions with per-region replica bounds.
    Omit to leave regions unchanged; when present, this replaces the full set
    (regions absent from the list are removed). At least one region is required;
    an empty list is rejected because an environment cannot have zero regions.

    """


class V2EnvironmentsUpdateSettingsRequestBody(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    dockerfile: OptionalNullable[str] = UNSET
    r"""Path to the Dockerfile used for builds.
    Omit to leave unchanged; set null to clear and fall back to Railpack.

    """

    root_directory: Annotated[Optional[str], pydantic.Field(alias="rootDirectory")] = (
        None
    )
    r"""The directory your app lives in. Unkey builds from here.
    Use \".\" for the repository root, or set a subdirectory when your app
    is nested (e.g., services/api). Omit to leave unchanged.

    """

    build_command: Annotated[
        OptionalNullable[str], pydantic.Field(alias="buildCommand")
    ] = UNSET
    r"""Overrides the build command auto-detected by Railpack.
    Omit to leave unchanged; set null to clear and fall back to auto-detection.

    """

    watch_paths: Annotated[Optional[List[str]], pydantic.Field(alias="watchPaths")] = (
        None
    )
    r"""Glob paths that trigger auto-deploys when changed.
    Omit to leave unchanged.

    """

    auto_deploy: Annotated[Optional[bool], pydantic.Field(alias="autoDeploy")] = None
    r"""Whether pushes auto-deploy.
    Omit to leave unchanged.

    """

    port: Optional[int] = None
    r"""Container port the app listens on.
    Omit to leave unchanged.

    """

    v_cpus: Annotated[Optional[float], pydantic.Field(alias="vCpus")] = None
    r"""CPU allocation in vCPUs. Minimum 0.25 (1/4 vCPU), in steps of 0.25.
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """

    memory_mib: Annotated[Optional[int], pydantic.Field(alias="memoryMib")] = None
    r"""Memory allocation in MiB. Minimum 256, in steps of 256.
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """

    storage_mib: Annotated[Optional[int], pydantic.Field(alias="storageMib")] = None
    r"""Ephemeral storage allocation in MiB, in steps of 512 (0 for none).
    The upper bound is your workspace's per-instance quota; exceeding it returns 400.
    Omit to leave unchanged.

    """

    command: Optional[List[str]] = None
    r"""Override container entrypoint command.
    Omit to leave unchanged.

    """

    healthcheck: OptionalNullable[EnvironmentHealthcheck] = UNSET
    r"""HTTP healthcheck configuration.
    Omit to leave unchanged; set null to remove.

    """

    shutdown_signal: Annotated[
        Optional[EnvironmentShutdownSignal], pydantic.Field(alias="shutdownSignal")
    ] = None
    r"""Signal sent to the container on shutdown.

    """

    upstream_protocol: Annotated[
        Optional[EnvironmentUpstreamProtocol], pydantic.Field(alias="upstreamProtocol")
    ] = None
    r"""Protocol used to reach the container.

    """

    openapi_spec_path: Annotated[
        OptionalNullable[str], pydantic.Field(alias="openapiSpecPath")
    ] = UNSET
    r"""Path to the OpenAPI spec file within the build. Must start with a slash.
    Omit to leave unchanged; set null to clear.

    """

    regions: Optional[List[EnvironmentRegion]] = None
    r"""Desired set of regions with per-region replica bounds.
    Omit to leave regions unchanged; when present, this replaces the full set
    (regions absent from the list are removed). At least one region is required;
    an empty list is rejected because an environment cannot have zero regions.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "dockerfile",
                "rootDirectory",
                "buildCommand",
                "watchPaths",
                "autoDeploy",
                "port",
                "vCpus",
                "memoryMib",
                "storageMib",
                "command",
                "healthcheck",
                "shutdownSignal",
                "upstreamProtocol",
                "openapiSpecPath",
                "regions",
            ]
        )
        nullable_fields = set(
            ["dockerfile", "buildCommand", "healthcheck", "openapiSpecPath"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    V2EnvironmentsUpdateSettingsRequestBody.model_rebuild()
except NameError:
    pass
