"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .deploymentsourcedeployment import (
    DeploymentSourceDeployment,
    DeploymentSourceDeploymentTypedDict,
)
from .deploymentsourcegit import DeploymentSourceGit, DeploymentSourceGitTypedDict
from .deploymentsourceimage import DeploymentSourceImage, DeploymentSourceImageTypedDict
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2DeploymentsCreateDeploymentRequestBody3TypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    deployment: DeploymentSourceDeploymentTypedDict
    r"""Re-run an existing deployment."""
    git: NotRequired[DeploymentSourceGitTypedDict]
    r"""Build from the app's connected GitHub repository."""
    image: NotRequired[DeploymentSourceImageTypedDict]
    r"""Deploy a prebuilt Docker image as-is."""


class V2DeploymentsCreateDeploymentRequestBody3(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    deployment: DeploymentSourceDeployment
    r"""Re-run an existing deployment."""

    git: Optional[DeploymentSourceGit] = None
    r"""Build from the app's connected GitHub repository."""

    image: Optional[DeploymentSourceImage] = None
    r"""Deploy a prebuilt Docker image as-is."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["git", "image"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2DeploymentsCreateDeploymentRequestBody2TypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    git: DeploymentSourceGitTypedDict
    r"""Build from the app's connected GitHub repository."""
    image: NotRequired[DeploymentSourceImageTypedDict]
    r"""Deploy a prebuilt Docker image as-is."""
    deployment: NotRequired[DeploymentSourceDeploymentTypedDict]
    r"""Re-run an existing deployment."""


class V2DeploymentsCreateDeploymentRequestBody2(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    git: DeploymentSourceGit
    r"""Build from the app's connected GitHub repository."""

    image: Optional[DeploymentSourceImage] = None
    r"""Deploy a prebuilt Docker image as-is."""

    deployment: Optional[DeploymentSourceDeployment] = None
    r"""Re-run an existing deployment."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["image", "deployment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2DeploymentsCreateDeploymentRequestBody1TypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    image: DeploymentSourceImageTypedDict
    r"""Deploy a prebuilt Docker image as-is."""
    git: NotRequired[DeploymentSourceGitTypedDict]
    r"""Build from the app's connected GitHub repository."""
    deployment: NotRequired[DeploymentSourceDeploymentTypedDict]
    r"""Re-run an existing deployment."""


class V2DeploymentsCreateDeploymentRequestBody1(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    image: DeploymentSourceImage
    r"""Deploy a prebuilt Docker image as-is."""

    git: Optional[DeploymentSourceGit] = None
    r"""Build from the app's connected GitHub repository."""

    deployment: Optional[DeploymentSourceDeployment] = None
    r"""Re-run an existing deployment."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["git", "deployment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


V2DeploymentsCreateDeploymentRequestBodyUnionTypedDict = TypeAliasType(
    "V2DeploymentsCreateDeploymentRequestBodyUnionTypedDict",
    Union[
        V2DeploymentsCreateDeploymentRequestBody1TypedDict,
        V2DeploymentsCreateDeploymentRequestBody2TypedDict,
        V2DeploymentsCreateDeploymentRequestBody3TypedDict,
    ],
)
r"""Create a deployment. Provide exactly one of git, image, or deployment."""


V2DeploymentsCreateDeploymentRequestBodyUnion = TypeAliasType(
    "V2DeploymentsCreateDeploymentRequestBodyUnion",
    Union[
        V2DeploymentsCreateDeploymentRequestBody1,
        V2DeploymentsCreateDeploymentRequestBody2,
        V2DeploymentsCreateDeploymentRequestBody3,
    ],
)
r"""Create a deployment. Provide exactly one of git, image, or deployment."""
