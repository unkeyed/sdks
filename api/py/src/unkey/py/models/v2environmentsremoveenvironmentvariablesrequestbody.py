"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from typing import List
from typing_extensions import TypedDict
from unkey.py.types import BaseModel


class V2EnvironmentsRemoveEnvironmentVariablesRequestBodyTypedDict(TypedDict):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    variables: List[str]
    r"""The names of the variables to remove. Keys that exist are deleted; keys
    that are not present are ignored, since their absence already matches the
    requested state.

    Duplicate keys collapse to a single removal. The whole operation is atomic:
    if any part fails the environment is left unchanged. Limited to 50
    variables per request.

    """


class V2EnvironmentsRemoveEnvironmentVariablesRequestBody(BaseModel):
    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    variables: List[str]
    r"""The names of the variables to remove. Keys that exist are deleted; keys
    that are not present are ignored, since their absence already matches the
    requested state.

    Duplicate keys collapse to a single removal. The whole operation is atomic:
    if any part fails the environment is left unchanged. Limited to 50
    variables per request.

    """
