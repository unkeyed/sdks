"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .authenticatedsubjectkey import (
    AuthenticatedSubjectKey,
    AuthenticatedSubjectKeyTypedDict,
)
from .headerkey import HeaderKey, HeaderKeyTypedDict
from .pathkey import PathKey, PathKeyTypedDict
from .principalfieldkey import PrincipalFieldKey, PrincipalFieldKeyTypedDict
from .ratelimitidentifier import RatelimitIdentifier, RatelimitIdentifierTypedDict
from .remoteipkey import RemoteIPKey, RemoteIPKeyTypedDict
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict, deprecated
from unkey.py.types import BaseModel, UNSET_SENTINEL


@deprecated(
    "warning: ** DEPRECATED ** - This will be removed in a future release, please migrate away from it as soon as possible."
)
class IdentifierTypedDict(TypedDict):
    r"""Deprecated. Accepted for compatibility with old clients. Use
    `identifiers` with one entry. Responses always return `identifiers`.
    """

    remote_ip: NotRequired[RemoteIPKeyTypedDict]
    r"""Rate limit by the client's IP address."""
    header: NotRequired[HeaderKeyTypedDict]
    r"""Rate limit by the value of a request header."""
    authenticated_subject: NotRequired[AuthenticatedSubjectKeyTypedDict]
    r"""Rate limit by the authenticated subject (e.g. the verified key)."""
    path: NotRequired[PathKeyTypedDict]
    r"""Rate limit by the request path."""
    principal_field: NotRequired[PrincipalFieldKeyTypedDict]
    r"""Rate limit by a field extracted from the authenticated principal."""


@deprecated(
    "warning: ** DEPRECATED ** - This will be removed in a future release, please migrate away from it as soon as possible."
)
class Identifier(BaseModel):
    r"""Deprecated. Accepted for compatibility with old clients. Use
    `identifiers` with one entry. Responses always return `identifiers`.
    """

    remote_ip: Annotated[Optional[RemoteIPKey], pydantic.Field(alias="remoteIp")] = None
    r"""Rate limit by the client's IP address."""

    header: Optional[HeaderKey] = None
    r"""Rate limit by the value of a request header."""

    authenticated_subject: Annotated[
        Optional[AuthenticatedSubjectKey], pydantic.Field(alias="authenticatedSubject")
    ] = None
    r"""Rate limit by the authenticated subject (e.g. the verified key)."""

    path: Optional[PathKey] = None
    r"""Rate limit by the request path."""

    principal_field: Annotated[
        Optional[PrincipalFieldKey], pydantic.Field(alias="principalField")
    ] = None
    r"""Rate limit by a field extracted from the authenticated principal."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["remoteIp", "header", "authenticatedSubject", "path", "principalField"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class RatelimitPolicyTypedDict(TypedDict):
    r"""Rate limits matching requests. Set `identifiers` with 1 to 5 sources.
    The deprecated `identifier` field is accepted in place of a one-entry
    `identifiers` list; set exactly one of the two.
    """

    limit: int
    r"""Maximum number of requests per window."""
    window_ms: int
    r"""Window duration in milliseconds."""
    identifier: NotRequired[IdentifierTypedDict]
    r"""Deprecated. Accepted for compatibility with old clients. Use
    `identifiers` with one entry. Responses always return `identifiers`.
    """
    identifiers: NotRequired[List[RatelimitIdentifierTypedDict]]
    r"""Ordered list of sources that form a compound rate limit key. The
    gateway resolves each source for each request. Each unique
    combination of resolved values has its own counter. All counters use
    the same limit and window. Example: `[authenticatedSubject, path]`
    limits each subject separately on each path.
    """


class RatelimitPolicy(BaseModel):
    r"""Rate limits matching requests. Set `identifiers` with 1 to 5 sources.
    The deprecated `identifier` field is accepted in place of a one-entry
    `identifiers` list; set exactly one of the two.
    """

    limit: int
    r"""Maximum number of requests per window."""

    window_ms: Annotated[int, pydantic.Field(alias="windowMs")]
    r"""Window duration in milliseconds."""

    identifier: Annotated[
        Optional[Identifier],
        pydantic.Field(
            deprecated="warning: ** DEPRECATED ** - This will be removed in a future release, please migrate away from it as soon as possible."
        ),
    ] = None
    r"""Deprecated. Accepted for compatibility with old clients. Use
    `identifiers` with one entry. Responses always return `identifiers`.
    """

    identifiers: Optional[List[RatelimitIdentifier]] = None
    r"""Ordered list of sources that form a compound rate limit key. The
    gateway resolves each source for each request. Each unique
    combination of resolved values has its own counter. All counters use
    the same limit and window. Example: `[authenticatedSubject, path]`
    limits each subject separately on each path.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["identifier", "identifiers"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Identifier.model_rebuild()
except NameError:
    pass
try:
    RatelimitPolicy.model_rebuild()
except NameError:
    pass
