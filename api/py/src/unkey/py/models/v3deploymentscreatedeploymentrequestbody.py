"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .deploymentsourcedeployment import (
    DeploymentSourceDeployment,
    DeploymentSourceDeploymentTypedDict,
)
from .deploymentsourcegit import DeploymentSourceGit, DeploymentSourceGitTypedDict
from .deploymentsourceoci import DeploymentSourceOCI, DeploymentSourceOCITypedDict
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V3DeploymentsCreateDeploymentRequestBodyTypedDict(TypedDict):
    r"""Create a deployment. Omit the source to use the app default, or provide one source override."""

    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    git: NotRequired[DeploymentSourceGitTypedDict]
    r"""Build from the app's connected GitHub repository."""
    oci: NotRequired[DeploymentSourceOCITypedDict]
    r"""Deploy a prebuilt OCI image without a build."""
    deployment: NotRequired[DeploymentSourceDeploymentTypedDict]
    r"""Re-run an existing deployment."""


class V3DeploymentsCreateDeploymentRequestBody(BaseModel):
    r"""Create a deployment. Omit the source to use the app default, or provide one source override."""

    project: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    app: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    environment: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    git: Optional[DeploymentSourceGit] = None
    r"""Build from the app's connected GitHub repository."""

    oci: Optional[DeploymentSourceOCI] = None
    r"""Deploy a prebuilt OCI image without a build."""

    deployment: Optional[DeploymentSourceDeployment] = None
    r"""Re-run an existing deployment."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["git", "oci", "deployment"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
