"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2PortalGetVerificationsRequestBodyTypedDict(TypedDict):
    start_time: int
    r"""Start of the query window as a unix timestamp in milliseconds (inclusive).

    """
    end_time: int
    r"""End of the query window as a unix timestamp in milliseconds (exclusive).
    Bucket granularity (minute, hour, or day) is chosen automatically from the
    window size.

    """
    key_id: NotRequired[str]
    r"""Optional. Restrict results to a single key. The key must belong to the
    authenticated end user; results are always scoped to the session identity
    regardless of this value.

    """


class V2PortalGetVerificationsRequestBody(BaseModel):
    start_time: Annotated[int, pydantic.Field(alias="startTime")]
    r"""Start of the query window as a unix timestamp in milliseconds (inclusive).

    """

    end_time: Annotated[int, pydantic.Field(alias="endTime")]
    r"""End of the query window as a unix timestamp in milliseconds (exclusive).
    Bucket granularity (minute, hour, or day) is chosen automatically from the
    window size.

    """

    key_id: Annotated[Optional[str], pydantic.Field(alias="keyId")] = None
    r"""Optional. Restrict results to a single key. The key must belong to the
    authenticated end user; results are always scoped to the session identity
    regardless of this value.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["keyId"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    V2PortalGetVerificationsRequestBody.model_rebuild()
except NameError:
    pass
