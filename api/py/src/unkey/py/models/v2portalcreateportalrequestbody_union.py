"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class V2PortalCreatePortalRequestBody2TypedDict(TypedDict):
    slug: str
    r"""URL-safe handle for this portal, unique within your workspace. Lowercase
    alphanumerics and single hyphens, 3-64 characters, not starting or ending
    with a hyphen.

    """
    display_name: str
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """
    app_id: str
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """
    keyspace_id: NotRequired[str]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """
    enabled: NotRequired[bool]
    r"""Whether sessions can be minted for this portal immediately. Defaults to
    true; send false to create it dormant and enable it later.

    """
    logo_url: NotRequired[str]
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """
    primary_color: NotRequired[str]
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """


class V2PortalCreatePortalRequestBody2(BaseModel):
    slug: str
    r"""URL-safe handle for this portal, unique within your workspace. Lowercase
    alphanumerics and single hyphens, 3-64 characters, not starting or ending
    with a hyphen.

    """

    display_name: Annotated[str, pydantic.Field(alias="displayName")]
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """

    app_id: Annotated[str, pydantic.Field(alias="appId")]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    keyspace_id: Annotated[Optional[str], pydantic.Field(alias="keyspaceId")] = None
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    enabled: Optional[bool] = True
    r"""Whether sessions can be minted for this portal immediately. Defaults to
    true; send false to create it dormant and enable it later.

    """

    logo_url: Annotated[Optional[str], pydantic.Field(alias="logoUrl")] = None
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """

    primary_color: Annotated[Optional[str], pydantic.Field(alias="primaryColor")] = None
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["keyspaceId", "enabled", "logoUrl", "primaryColor"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class V2PortalCreatePortalRequestBody1TypedDict(TypedDict):
    slug: str
    r"""URL-safe handle for this portal, unique within your workspace. Lowercase
    alphanumerics and single hyphens, 3-64 characters, not starting or ending
    with a hyphen.

    """
    display_name: str
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """
    keyspace_id: str
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """
    app_id: NotRequired[str]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """
    enabled: NotRequired[bool]
    r"""Whether sessions can be minted for this portal immediately. Defaults to
    true; send false to create it dormant and enable it later.

    """
    logo_url: NotRequired[str]
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """
    primary_color: NotRequired[str]
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """


class V2PortalCreatePortalRequestBody1(BaseModel):
    slug: str
    r"""URL-safe handle for this portal, unique within your workspace. Lowercase
    alphanumerics and single hyphens, 3-64 characters, not starting or ending
    with a hyphen.

    """

    display_name: Annotated[str, pydantic.Field(alias="displayName")]
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """

    keyspace_id: Annotated[str, pydantic.Field(alias="keyspaceId")]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    app_id: Annotated[Optional[str], pydantic.Field(alias="appId")] = None
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    enabled: Optional[bool] = True
    r"""Whether sessions can be minted for this portal immediately. Defaults to
    true; send false to create it dormant and enable it later.

    """

    logo_url: Annotated[Optional[str], pydantic.Field(alias="logoUrl")] = None
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """

    primary_color: Annotated[Optional[str], pydantic.Field(alias="primaryColor")] = None
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["appId", "enabled", "logoUrl", "primaryColor"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


V2PortalCreatePortalRequestBodyUnionTypedDict = TypeAliasType(
    "V2PortalCreatePortalRequestBodyUnionTypedDict",
    Union[
        V2PortalCreatePortalRequestBody1TypedDict,
        V2PortalCreatePortalRequestBody2TypedDict,
    ],
)


V2PortalCreatePortalRequestBodyUnion = TypeAliasType(
    "V2PortalCreatePortalRequestBodyUnion",
    Union[V2PortalCreatePortalRequestBody1, V2PortalCreatePortalRequestBody2],
)


try:
    V2PortalCreatePortalRequestBody2.model_rebuild()
except NameError:
    pass
try:
    V2PortalCreatePortalRequestBody1.model_rebuild()
except NameError:
    pass
