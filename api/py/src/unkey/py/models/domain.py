"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .dnsrecord import DNSRecord, DNSRecordTypedDict
from enum import Enum
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class DomainStatus(str, Enum):
    r"""The verification status of the domain.

    - `pending`: the domain is created. No DNS check has completed yet.
    - `verifying`: Unkey checks the DNS records approximately each minute.
    - `verified`: the domain is verified. Unkey has configured routing and requested a certificate.
    - `failed`: the required DNS records did not appear within 24 hours. Fix the records, then retry verification.

    """

    PENDING = "pending"
    VERIFYING = "verifying"
    VERIFIED = "verified"
    FAILED = "failed"


class DomainTypedDict(TypedDict):
    id: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    domain: str
    r"""Fully qualified domain name attached to the environment."""
    project_id: str
    r"""The project the domain's environment belongs to."""
    app_id: str
    r"""The app the domain's environment belongs to."""
    environment_id: str
    r"""The environment this domain serves. Traffic to the domain reaches whatever is currently
    deployed to this environment.

    """
    status: DomainStatus
    r"""The verification status of the domain.

    - `pending`: the domain is created. No DNS check has completed yet.
    - `verifying`: Unkey checks the DNS records approximately each minute.
    - `verified`: the domain is verified. Unkey has configured routing and requested a certificate.
    - `failed`: the required DNS records did not appear within 24 hours. Fix the records, then retry verification.

    """
    dns_records: List[DNSRecordTypedDict]
    r"""The DNS records this domain needs. Create each record at your DNS provider.
    Each record has a `verified` flag. The flag shows whether Unkey has read that record back,
    so it tells you which records are still missing.

    """
    created_at: int
    r"""Unix timestamp in milliseconds when the domain was created. The 24 hour verification window runs from here."""
    verification_error: NotRequired[str]
    r"""Why the most recent verification attempt did not succeed, in plain language.
    Omitted while verification is progressing normally.

    """
    updated_at: NotRequired[int]
    r"""Unix timestamp in milliseconds of the last change to this domain. Omitted if it has never changed."""


class Domain(BaseModel):
    id: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    domain: str
    r"""Fully qualified domain name attached to the environment."""

    project_id: Annotated[str, pydantic.Field(alias="projectId")]
    r"""The project the domain's environment belongs to."""

    app_id: Annotated[str, pydantic.Field(alias="appId")]
    r"""The app the domain's environment belongs to."""

    environment_id: Annotated[str, pydantic.Field(alias="environmentId")]
    r"""The environment this domain serves. Traffic to the domain reaches whatever is currently
    deployed to this environment.

    """

    status: DomainStatus
    r"""The verification status of the domain.

    - `pending`: the domain is created. No DNS check has completed yet.
    - `verifying`: Unkey checks the DNS records approximately each minute.
    - `verified`: the domain is verified. Unkey has configured routing and requested a certificate.
    - `failed`: the required DNS records did not appear within 24 hours. Fix the records, then retry verification.

    """

    dns_records: Annotated[List[DNSRecord], pydantic.Field(alias="dnsRecords")]
    r"""The DNS records this domain needs. Create each record at your DNS provider.
    Each record has a `verified` flag. The flag shows whether Unkey has read that record back,
    so it tells you which records are still missing.

    """

    created_at: Annotated[int, pydantic.Field(alias="createdAt")]
    r"""Unix timestamp in milliseconds when the domain was created. The 24 hour verification window runs from here."""

    verification_error: Annotated[
        Optional[str], pydantic.Field(alias="verificationError")
    ] = None
    r"""Why the most recent verification attempt did not succeed, in plain language.
    Omitted while verification is progressing normally.

    """

    updated_at: Annotated[Optional[int], pydantic.Field(alias="updatedAt")] = None
    r"""Unix timestamp in milliseconds of the last change to this domain. Omitted if it has never changed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["verificationError", "updatedAt"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Domain.model_rebuild()
except NameError:
    pass
