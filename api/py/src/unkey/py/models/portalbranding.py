"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class PortalBrandingTypedDict(TypedDict):
    r"""How the portal looks to your end users. Both fields are optional; a portal
    with neither set renders with default styling.

    """

    logo_url: NotRequired[str]
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """
    primary_color: NotRequired[str]
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """


class PortalBranding(BaseModel):
    r"""How the portal looks to your end users. Both fields are optional; a portal
    with neither set renders with default styling.

    """

    logo_url: Annotated[Optional[str], pydantic.Field(alias="logoUrl")] = None
    r"""Absolute `https://` URL of the logo shown in the portal header.

    Loaded by your end users' browsers, so the host you name receives their IP
    and user agent on every page view.

    """

    primary_color: Annotated[Optional[str], pydantic.Field(alias="primaryColor")] = None
    r"""Six-digit hex colour used for primary actions and accents in the portal.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["logoUrl", "primaryColor"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    PortalBranding.model_rebuild()
except NameError:
    pass
