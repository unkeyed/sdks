"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .portalbranding import PortalBranding, PortalBrandingTypedDict
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class PortalTypedDict(TypedDict):
    r"""A portal you expose to your end users so they can manage their own keys.

    Exactly one of `keyspaceId` or `appId` is present, naming the single resource
    the portal serves. Neither is in the required list because which one appears
    depends on the portal, so a reader checks for the one it cares about.

    """

    id: str
    r"""The unique identifier of the portal, generated by Unkey.

    """
    slug: str
    r"""URL-safe handle for this portal, unique within your workspace.

    """
    display_name: str
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """
    enabled: bool
    r"""Whether new sessions can be minted for this portal.

    Disabling a portal stops `portal.createSession` from minting new sessions.
    It does not end sessions that are already live.

    """
    created_at: int
    r"""Unix timestamp in milliseconds when the portal was created.

    """
    keyspace_id: NotRequired[str]
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """
    app_id: NotRequired[str]
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """
    branding: NotRequired[PortalBrandingTypedDict]
    r"""How the portal looks to your end users. Omitted when no branding is set.

    """
    updated_at: NotRequired[int]
    r"""Unix timestamp in milliseconds when the portal was last changed. Omitted
    if it has never been changed since creation.

    """


class Portal(BaseModel):
    r"""A portal you expose to your end users so they can manage their own keys.

    Exactly one of `keyspaceId` or `appId` is present, naming the single resource
    the portal serves. Neither is in the required list because which one appears
    depends on the portal, so a reader checks for the one it cares about.

    """

    id: str
    r"""The unique identifier of the portal, generated by Unkey.

    """

    slug: str
    r"""URL-safe handle for this portal, unique within your workspace.

    """

    display_name: Annotated[str, pydantic.Field(alias="displayName")]
    r"""Human-readable name your end users see in the portal header and page
    titles. Unlike `slug` it is not part of any URL, so it can be changed
    freely.

    """

    enabled: bool
    r"""Whether new sessions can be minted for this portal.

    Disabling a portal stops `portal.createSession` from minting new sessions.
    It does not end sessions that are already live.

    """

    created_at: Annotated[int, pydantic.Field(alias="createdAt")]
    r"""Unix timestamp in milliseconds when the portal was created.

    """

    keyspace_id: Annotated[Optional[str], pydantic.Field(alias="keyspaceId")] = None
    r"""The id of the keyspace this portal serves keys for. Must belong to your
    workspace.

    A portal serves exactly one resource, so `keyspaceId` and `appId` are
    mutually exclusive.

    """

    app_id: Annotated[Optional[str], pydantic.Field(alias="appId")] = None
    r"""The id of the app this portal serves keys for. Must belong to your workspace.

    A portal serves exactly one resource, so `appId` and `keyspaceId` are mutually
    exclusive.

    """

    branding: Optional[PortalBranding] = None
    r"""How the portal looks to your end users. Omitted when no branding is set.

    """

    updated_at: Annotated[Optional[int], pydantic.Field(alias="updatedAt")] = None
    r"""Unix timestamp in milliseconds when the portal was last changed. Omitted
    if it has never been changed since creation.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["keyspaceId", "appId", "branding", "updatedAt"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Portal.model_rebuild()
except NameError:
    pass
