"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, Nullable, OptionalNullable, UNSET, UNSET_SENTINEL


class V2PortalUpdatePortalRequestBodyTypedDict(TypedDict):
    portal: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """
    slug: NotRequired[str]
    r"""New handle for this portal. Omit to leave unchanged.

    """
    display_name: NotRequired[str]
    r"""New human-readable name shown to your end users. Omit to leave unchanged.

    """
    keyspace_id: NotRequired[str]
    r"""Re-point the portal at a different keyspace. Omit to leave the resource it
    serves alone.

    Re-pointing revokes the portal's live sessions, because a session carries
    the keyspace scope it was minted with and would otherwise keep reaching the
    resource the portal no longer serves.

    """
    app_id: NotRequired[str]
    r"""Re-point the portal at a different app. Omit to leave the resource it
    serves alone.

    Re-pointing revokes the portal's live sessions, for the same reason as
    `keyspaceId`.

    """
    enabled: NotRequired[bool]
    r"""Whether new sessions can be minted. Omit to leave unchanged.

    Disabling does not end sessions that are already live.

    """
    logo_url: NotRequired[Nullable[str]]
    r"""Absolute `https://` URL of the portal logo. Omit to leave unchanged, or set
    null to remove the logo.

    """
    primary_color: NotRequired[Nullable[str]]
    r"""Six-digit hex colour for primary actions. Omit to leave unchanged, or set
    null to fall back to default styling.

    """


class V2PortalUpdatePortalRequestBody(BaseModel):
    portal: str
    r"""Identifies a resource by either its unique ID or its slug.
    Accepts a prefixed ID (such as 'proj_' or 'app_') or a slug.

    """

    slug: Optional[str] = None
    r"""New handle for this portal. Omit to leave unchanged.

    """

    display_name: Annotated[Optional[str], pydantic.Field(alias="displayName")] = None
    r"""New human-readable name shown to your end users. Omit to leave unchanged.

    """

    keyspace_id: Annotated[Optional[str], pydantic.Field(alias="keyspaceId")] = None
    r"""Re-point the portal at a different keyspace. Omit to leave the resource it
    serves alone.

    Re-pointing revokes the portal's live sessions, because a session carries
    the keyspace scope it was minted with and would otherwise keep reaching the
    resource the portal no longer serves.

    """

    app_id: Annotated[Optional[str], pydantic.Field(alias="appId")] = None
    r"""Re-point the portal at a different app. Omit to leave the resource it
    serves alone.

    Re-pointing revokes the portal's live sessions, for the same reason as
    `keyspaceId`.

    """

    enabled: Optional[bool] = None
    r"""Whether new sessions can be minted. Omit to leave unchanged.

    Disabling does not end sessions that are already live.

    """

    logo_url: Annotated[OptionalNullable[str], pydantic.Field(alias="logoUrl")] = UNSET
    r"""Absolute `https://` URL of the portal logo. Omit to leave unchanged, or set
    null to remove the logo.

    """

    primary_color: Annotated[
        OptionalNullable[str], pydantic.Field(alias="primaryColor")
    ] = UNSET
    r"""Six-digit hex colour for primary actions. Omit to leave unchanged, or set
    null to fall back to default styling.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "slug",
                "displayName",
                "keyspaceId",
                "appId",
                "enabled",
                "logoUrl",
                "primaryColor",
            ]
        )
        nullable_fields = set(["logoUrl", "primaryColor"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    V2PortalUpdatePortalRequestBody.model_rebuild()
except NameError:
    pass
