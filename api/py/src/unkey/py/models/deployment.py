"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .deploymentaction import DeploymentAction
from .deploymentdocker import DeploymentDocker, DeploymentDockerTypedDict
from .deploymenterror import DeploymentError, DeploymentErrorTypedDict
from .deploymentgit import DeploymentGit, DeploymentGitTypedDict
from .deploymentruntime import DeploymentRuntime, DeploymentRuntimeTypedDict
from .deploymentstatus import DeploymentStatus
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict
from unkey.py.types import BaseModel, UNSET_SENTINEL


class DeploymentTypedDict(TypedDict):
    id: str
    r"""The unique identifier of the deployment, generated by Unkey.

    """
    status: DeploymentStatus
    r"""Current lifecycle status of the deployment. Poll until it reaches a
    terminal state: ready (serving), failed, skipped, superseded, stopped,
    or cancelled.

    """
    is_current: bool
    r"""True when this is the production deployment currently serving traffic, i.e.
    on api.acme.com. Only production deployments can be current, and at most one
    deployment is current. Rollbacks and promotions change which deployment is
    current and serves requests to api.acme.com.

    """
    environment: str
    r"""Slug of the environment this deployment belongs to."""
    app: str
    r"""Slug of the app this deployment belongs to."""
    project: str
    r"""Slug of the project this deployment belongs to."""
    available_actions: List[DeploymentAction]
    r"""Lifecycle operations you are allowed to call on this deployment right now.
    Empty when none apply (e.g. while building or in a terminal state).

    """
    regions: List[str]
    r"""Regions this deployment is configured to run in. Empty while the deployment
    has no scheduled regions yet.

    """
    runtime: DeploymentRuntimeTypedDict
    created_at: int
    r"""Unix timestamp in milliseconds when the deployment was created.

    """
    git: NotRequired[DeploymentGitTypedDict]
    docker: NotRequired[DeploymentDockerTypedDict]
    error: NotRequired[DeploymentErrorTypedDict]
    domains: NotRequired[List[str]]
    r"""Public hostnames this deployment is reachable at.

    """
    updated_at: NotRequired[int]
    r"""Unix timestamp in milliseconds when the deployment was last updated.
    Omitted if the deployment has never been updated.

    """


class Deployment(BaseModel):
    id: str
    r"""The unique identifier of the deployment, generated by Unkey.

    """

    status: DeploymentStatus
    r"""Current lifecycle status of the deployment. Poll until it reaches a
    terminal state: ready (serving), failed, skipped, superseded, stopped,
    or cancelled.

    """

    is_current: Annotated[bool, pydantic.Field(alias="isCurrent")]
    r"""True when this is the production deployment currently serving traffic, i.e.
    on api.acme.com. Only production deployments can be current, and at most one
    deployment is current. Rollbacks and promotions change which deployment is
    current and serves requests to api.acme.com.

    """

    environment: str
    r"""Slug of the environment this deployment belongs to."""

    app: str
    r"""Slug of the app this deployment belongs to."""

    project: str
    r"""Slug of the project this deployment belongs to."""

    available_actions: Annotated[
        List[DeploymentAction], pydantic.Field(alias="availableActions")
    ]
    r"""Lifecycle operations you are allowed to call on this deployment right now.
    Empty when none apply (e.g. while building or in a terminal state).

    """

    regions: List[str]
    r"""Regions this deployment is configured to run in. Empty while the deployment
    has no scheduled regions yet.

    """

    runtime: DeploymentRuntime

    created_at: Annotated[int, pydantic.Field(alias="createdAt")]
    r"""Unix timestamp in milliseconds when the deployment was created.

    """

    git: Optional[DeploymentGit] = None

    docker: Optional[DeploymentDocker] = None

    error: Optional[DeploymentError] = None

    domains: Optional[List[str]] = None
    r"""Public hostnames this deployment is reachable at.

    """

    updated_at: Annotated[Optional[int], pydantic.Field(alias="updatedAt")] = None
    r"""Unix timestamp in milliseconds when the deployment was last updated.
    Omitted if the deployment has never been updated.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["git", "docker", "error", "domains", "updatedAt"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Deployment.model_rebuild()
except NameError:
    pass
